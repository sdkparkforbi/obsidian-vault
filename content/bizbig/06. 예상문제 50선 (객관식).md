---
title: 06. 예상문제 50선 (객관식)
---

# 06. 예상문제 50선 (객관식)

> 경영빅데이터분석(BizBig) 5개 챕터의 **코드 동작과 출력(그림·표) 해석**을 묻는 객관식 50문항임. 단순 암기보다 "왜 그렇게 되는가", "이 출력이 무엇을 뜻하는가"를 통합적으로 묻도록 구성함.
>
> 각 문항의 **정답·해설은 접혀 있음**. 먼저 스스로 풀어 본 뒤 펼쳐서 확인할 것. (해설 상자를 클릭하면 펼쳐짐)

관련 학습: [[01. ETL — 데이터 추출·변환·적재]] · [[02. 데이터 정제]] · [[03. 탐색적 데이터 분석]] · [[04. 선형회귀]] · [[05. 시계열 분석]]

---

## A. ETL — 데이터 추출·변환·적재 (1~7)

**문제 1.** `filter(bike, registered == 0, season == 1 | season == 2)` 가 추출하는 행으로 옳은 것은?

- ① 비회원이거나, 봄 또는 여름이면, 셋 중 하나라도 해당하는 행
- ② 회원이면서 봄 또는 여름인 행
- ③ 비회원이면서, 봄 또는 여름인 행
- ④ 비회원이면서 봄이고 동시에 여름인 행

> [!success]- 정답 및 해설
> **정답: ③**
> `filter()`에서 조건을 **쉼표로 나열하면 AND**로 묶임. 따라서 `registered == 0`(비회원) **그리고** `(season == 1 | season == 2)`(봄 또는 여름)임. `|`는 OR이므로 두 조건이 결합되어 "비회원 + (봄 또는 여름)"이 됨. ④처럼 한 행이 동시에 봄이면서 여름일 수는 없으므로 오답. ①은 전체를 OR로 잘못 해석한 것.

**문제 2.** 노트북에서 `season == 1 | season == 2` 대신 `season %in% c(1, 2)`로 바꿔 실행한 뒤 `identical(extracted_rows, using_membership)` 결과가 `TRUE`로 나왔음. 이로부터 알 수 있는 것은?

- ① 두 표현식은 완전히 동일한 결과(값·구조 모두)를 만든다
- ② 두 표현식이 서로 다른 행을 뽑지만 행 개수만 같다
- ③ `%in%` 가 `|` 보다 더 많은 행을 뽑는다
- ④ `identical()` 은 행 개수만 비교한다

> [!success]- 정답 및 해설
> **정답: ①**
> `identical()`은 두 객체가 **값과 구조까지 완전히 같은지**를 엄격히 비교함. `TRUE`라면 `season %in% c(1,2)`와 `season == 1 | season == 2`가 만든 데이터프레임이 동일하다는 뜻임. `%in%`는 "포함 여부"를 간결하게 쓰는 표현일 뿐 결과는 OR 나열과 같음.

**문제 3.** 다음 파이프라인의 최종 결과 `report`의 **행 수**로 가장 적절한 것은?
```r
add_revenue <- mutate(extracted_columns, revenue = casual * 5)
grouped <- group_by(add_revenue, season)
report <- summarise(grouped, sum(casual), sum(revenue))
```
(단, `extracted_columns`에는 봄·여름 두 계절만 존재함)

- ① 원본 행 수와 동일
- ② 2행 (계절 수만큼)
- ③ 1행 (전체 합계)
- ④ 4행 (사계절)

> [!success]- 정답 및 해설
> **정답: ②**
> `group_by(season)` 후 `summarise()`는 **그룹(계절)마다 한 줄**로 요약함. 데이터에 봄·여름 두 계절만 있으므로 결과는 2행임. `summarise`는 그룹 단위로 집계하므로 원본 행 수와 무관함. 사계절이 다 있었다면 4행이 됐을 것.

**문제 4.** `write.csv(report, "...revenue_report.csv", row.names = FALSE)` 에서 `row.names = FALSE`를 지정하는 이유로 가장 적절한 것은?

- ① 저장 속도를 높이기 위해
- ② 첫 번째 열의 데이터가 삭제되는 것을 막기 위해
- ③ 한글이 깨지는 것을 막기 위해
- ④ 행 번호가 별도 열로 저장되어 나중에 다시 읽을 때 불필요한 열이 생기는 것을 막기 위해

> [!success]- 정답 및 해설
> **정답: ④**
> 기본값(`row.names = TRUE`)이면 1, 2, 3… 같은 **행 이름이 이름 없는 첫 열로 저장**됨. 이 파일을 다시 `read.csv`로 읽으면 `X` 같은 의미 없는 열이 하나 더 생김. `FALSE`로 두면 이를 방지함. 실제 데이터 열이 지워지는 것(②)과는 무관함.

**문제 5.** 데이터를 읽은 직후 `str(bike)`를 실행하는 주된 목적은?

- ① 변수 개수·각 변수의 자료형·예시 값을 한눈에 확인해 이후 처리 방향을 정하기 위해
- ② 결측치의 개수를 정확히 세기 위해
- ③ 데이터를 정렬하기 위해
- ④ 그래프를 그리기 위해

> [!success]- 정답 및 해설
> **정답: ①**
> `str()`은 데이터프레임의 **구조(structure)** — 관측치/변수 수, 각 열의 자료형(int/num/chr/Factor), 앞부분 값 — 를 요약해 보여줌. 예컨대 숫자여야 할 `humidity`가 `chr`로 읽혔는지 등을 초기에 발견해 정제·변환 계획을 세울 수 있음. 결측치 개수는 `table(is.na())` 등으로 따로 셈.

**문제 6.** 이 노트북들은 Colab에서 **먼저 파이썬 런타임으로 드라이브를 마운트한 뒤 런타임을 R로 바꾸어** 분석을 진행함. 그 이유로 가장 적절한 것은?

- ① R에서는 데이터프레임을 쓸 수 없어서
- ② 파이썬이 R보다 CSV를 빠르게 읽기 때문
- ③ `drive.mount()` 가 파이썬 코드라 파이썬 런타임에서 실행해야 하고, 실제 분석 코드는 R로 작성되어 있기 때문
- ④ R 런타임에서는 그래프를 그릴 수 없어서

> [!success]- 정답 및 해설
> **정답: ③**
> `from google.colab import drive; drive.mount(...)`는 **파이썬 API**라 파이썬 런타임에서 실행함. 그 후 분석 본문은 R 코드(`read.csv`, `dplyr` 등)이므로 런타임을 R로 전환함. 즉 "마운트는 파이썬, 분석은 R"이라는 환경적 제약 때문임.

**문제 7.** `dplyr` 파이프라인을 `select(season, casual)` → `filter(registered == 0)` 순서로 (열 선택을 먼저) 작성하면 어떤 문제가 생기는가?

- ① 아무 문제 없이 동일한 결과가 나온다
- ② `select` 는 항상 `filter` 뒤에만 쓸 수 있다
- ③ 결과의 행 순서만 달라진다
- ④ `filter` 가 참조하는 `registered` 열이 이미 `select`로 제거되어 오류가 난다

> [!success]- 정답 및 해설
> **정답: ④**
> `select(season, casual)`로 두 열만 남기면 `registered` 열이 사라짐. 이후 `filter(registered == 0)`는 존재하지 않는 열을 참조하므로 오류가 남. **필터 조건에 쓰는 열은 그 시점까지 남아 있어야 함** — 그래서 보통 `filter`(행 거르기)를 먼저 하고 `select`(열 줄이기)를 나중에 함.

---

## B. 데이터 정제 (8~15)

**문제 8.** `str_detect(bike$sources, "NA")` 와 `is.na(bike$sources)` 의 차이를 가장 정확히 설명한 것은?

- ① 둘 다 진짜 결측치를 찾는다
- ② `str_detect`는 **글자 "NA"** 가 들어간 문자열을 찾고, `is.na`는 **R의 진짜 결측치(NA)** 를 찾는다
- ③ 둘 다 글자 "NA"만 찾는다
- ④ `is.na`는 숫자에만 쓸 수 있다

> [!success]- 정답 및 해설
> **정답: ②**
> 문자열 `"NA"`(엔·에이라는 **글자**)와 R의 결측 표시 `NA`(값의 부재)는 전혀 다름. `str_detect(x, "NA")`는 텍스트 패턴 "NA"가 들어 있는지를 검사하고, `is.na(x)`는 그 자리가 실제 결측인지를 검사함. 정제에서 이 둘을 구분하는 것이 첫걸음임.

**문제 9.** 원시 데이터에서 `humidity` 열이 숫자가 아니라 **문자(chr)** 로 읽힌 가장 그럴듯한 원인은?

- ① 값이 너무 커서
- ② 소수점이 있어서
- ③ 일부 칸에 알파벳 같은 잘못된 문자가 섞여 있어 열 전체가 문자형으로 인식됨
- ④ 결측치가 하나라도 있어서

> [!success]- 정답 및 해설
> **정답: ③**
> CSV의 한 열에 숫자와 문자가 섞이면 R은 그 열 전체를 **문자형**으로 읽음. 그래서 `str_subset(bike$humidity, "[a-z A-Z]")`로 알파벳이 섞인 값을 찾아내고, 고친 뒤 `as.numeric()`으로 변환함. 결측치나 소수점만으로는 문자형이 되지 않음.

**문제 10.** 다음 두 줄의 **실행 순서를 바꾸면**(as.numeric을 먼저, 오류값 교체를 나중에) 어떤 일이 벌어지는가?
```r
bike$humidity <- str_replace_all(bike$humidity, bad_data, "61")  # 오류값 교체
bike$humidity <- as.numeric(bike$humidity)                       # 숫자 변환
```

- ① 결과가 같다
- ② 변환을 먼저 해도 알파벳이 숫자로 잘 바뀐다
- ③ `str_replace_all` 이 숫자에는 동작하지 않아 오류가 난다
- ④ 변환을 먼저 하면 알파벳이 섞인 값이 `NA`로 바뀌어 원래 값이 손실된다

> [!success]- 정답 및 해설
> **정답: ④**
> `as.numeric("61x")`처럼 숫자로 바꿀 수 없는 문자열은 **경고와 함께 `NA`** 가 됨. 따라서 오류값을 고치기 **전에** `as.numeric()`을 먼저 하면 해당 칸이 `NA`로 변해 정보가 사라짐. **반드시 "오류값 교체 → 형 변환" 순서**여야 함. 정제에서 단계 순서가 결과를 좌우하는 대표적 예임.

**문제 11.** `factor(season, levels = c(1,2,3,4), labels = c("spring","summer","fall","winter"), ordered = TRUE)` 에서 `ordered = TRUE`가 갖는 효과로 옳은 것은?

- ① 계절을 알파벳 순으로 정렬한다
- ② 결측치를 자동으로 채운다
- ③ 범주 간에 크기 순서(spring < summer < fall < winter)가 부여되어 대소 비교·순서형 분석이 가능해진다
- ④ 숫자처럼 평균을 계산할 수 있게 한다

> [!success]- 정답 및 해설
> **정답: ③**
> `ordered = TRUE`는 **순서형 요인(ordered factor)** 을 만들어 범주 사이에 순서를 부여함. 그 결과 `season < "fall"` 같은 비교나 순서를 가정한 분석이 가능해짐. 다만 순서가 있다고 해서 평균을 내는 것이 의미 있는 것은 아님(④는 오답). 라벨 순서는 `levels` 지정 순서를 따름(알파벳 순 아님).

**문제 12.** 원본 날짜가 `01/20/2011 11:00` (월/일/년 시:분) 형식일 때 올바른 `lubridate` 함수는?

- ① `ymd_hms()`
- ② `mdy_hm()`
- ③ `dmy_hm()`
- ④ `ymd()`

> [!success]- 정답 및 해설
> **정답: ②**
> `lubridate` 함수 이름은 **구성 요소의 순서**를 그대로 따름. 월(m)-일(d)-년(y) 다음에 시(h):분(m)이므로 `mdy_hm()`임. 만약 `2011-01-20 11:00:00`이라면 `ymd_hms()`를 씀. 형식과 함수 이름을 일치시키는 것이 핵심.

**문제 13.** 정제 단계에서 `web_sites <- "(www.[a-z]*.[a-z]*)"` 패턴으로 여러 웹주소를 모두 `"web"` 하나로 바꾸는 작업의 의도로 가장 적절한 것은?

- ① 데이터 용량을 줄이기 위해
- ② 웹주소를 완전히 삭제하기 위해
- ③ 정규표현식 연습을 위해
- ④ 의미가 같은 여러 표기를 소수의 범주로 묶어(범주 수를 7±2 수준으로) 분석·해석을 쉽게 하기 위해

> [!success]- 정답 및 해설
> **정답: ④**
> 노트북 주석의 *"power of seven, plus or minus two"* 는 사람이 한 번에 다루기 좋은 범주 수가 약 7개(±2)라는 뜻임. `www.google.com`, `www.bing.com` 등 제각각인 출처를 `"web"` 한 범주로 묶으면 `sources`의 범주가 단순해져 표·그래프 해석이 쉬워짐. 이것이 **범주 표준화/축소**의 목적임.

**문제 14.** 문자열 표준화에서 `tolower()` → `str_trim()` → 결측치를 `"unknown"`으로 채우는 순서로 처리했음. `tolower()`를 가장 먼저 적용하는 이유로 적절한 것은?

- ① 대소문자만 다른 같은 값(`Google`, `google`)이 서로 다른 범주로 세어지는 것을 막기 위해
- ② 공백을 제거하기 위해
- ③ 결측치를 만들기 위해
- ④ 숫자를 문자로 바꾸기 위해

> [!success]- 정답 및 해설
> **정답: ①**
> `Google`과 `google`은 사람 눈엔 같지만 R에겐 다른 값임. 먼저 `tolower()`로 소문자로 통일하면 `unique()`·`factor`에서 하나의 범주로 합쳐짐. 그 뒤 `str_trim()`으로 앞뒤 공백을 없애고 결측은 `"unknown"`으로 채워 범주를 깔끔히 정리함.

**문제 15.** 다음 중 이 장에서 권장하는 **정제 작업의 올바른 큰 순서**는?

- ① 형 변환 → 오류값 탐지·수정 → 문자열 표준화
- ② 문자열 표준화 → 형 변환 → 오류값 탐지
- ③ 오류값 탐지·수정 → 형 변환 → 문자열 표준화
- ④ 형 변환 → 문자열 표준화 → 오류값 탐지

> [!success]- 정답 및 해설
> **정답: ③**
> 먼저 **이상한 값을 찾아 고치고**(예: humidity의 알파벳 → 61), 그다음 **형을 변환**(`as.numeric`, `factor`, 날짜)해야 변환 과정에서 값이 `NA`로 날아가지 않음. 문자열 표준화(소문자·공백·범주 묶기)는 범주형으로 굳히기 전에 마무리함. 12·10번 문항과 같은 맥락임.

---

## C. 탐색적 데이터 분석 (16~26)

**문제 16.** Anscombe의 4중주(anscombe) 예제가 주는 핵심 교훈은?

- ① 요약통계(평균·분산·상관)가 거의 같아도 실제 분포 모양은 완전히 다를 수 있으므로 반드시 시각화해야 한다
- ② 평균이 같으면 데이터도 같다
- ③ 상관계수가 높으면 항상 인과관계가 있다
- ④ 회귀직선은 어떤 데이터에도 잘 맞는다

> [!success]- 정답 및 해설
> **정답: ①**
> anscombe의 네 쌍은 평균·분산·상관·회귀직선이 거의 동일하지만, 그려 보면 (직선/곡선/이상치 하나가 지배/한 점이 기울기를 결정) 전혀 다른 모양임. **숫자 요약만 믿지 말고 그림으로 확인**하라는 것이 EDA의 출발 교훈임.

**문제 17.** 두 변수의 산점도에 `main = "Cor: 0.766"` 과 `main = "Cor: 0.076"` 이 각각 붙어 있음. 두 그림의 모양을 옳게 짝지은 것은?

- ① 0.766 그림이 더 둥근 구름, 0.076 그림이 더 직선적
- ② 둘 다 완전한 직선
- ③ 0.076 그림이 더 강한 직선 경향
- ④ 0.766 그림이 우상향 직선 경향이 뚜렷, 0.076 그림은 점들이 흩어진 구름 모양

> [!success]- 정답 및 해설
> **정답: ④**
> 상관계수의 절댓값이 1에 가까울수록 점들이 **직선에 가깝게** 모임. `0.766`은 비교적 뚜렷한 우상향 경향, `0.076`은 거의 무관해 **흩어진 구름** 모양임. 상관계수 크기와 산점도 모양을 연결해 읽는 것이 핵심.

**문제 18.** 상자그림(boxplot)에서 상자 위·아래 수염(whisker) 바깥에 따로 찍힌 점이 의미하는 것은?

- ① 결측치
- ② 이상치(outlier) 후보
- ③ 평균값
- ④ 중앙값

> [!success]- 정답 및 해설
> **정답: ②**
> 상자그림에서 상자는 1~3사분위(IQR), 가운데 굵은 선은 중앙값, 수염은 보통 1.5×IQR 범위를 나타냄. 그 **바깥의 점은 이상치 후보**로 표시됨. 분포의 치우침과 이상치를 동시에 보는 데 유용함.

**문제 19.** 다음 중 **범주형 변수 × 숫자형 변수**의 관계를 보기에 가장 적절한 그림은?

- ① 모자이크플롯(mosaicplot)
- ② 두 변수 산점도(plot)
- ③ 범주별 상자그림(boxplot)
- ④ 히스토그램 하나

> [!success]- 정답 및 해설
> **정답: ③**
> 자료형 조합별 적정 그림: 범주×범주 → 모자이크/교차표, **범주×숫자 → 범주별 상자그림**, 숫자×숫자 → 산점도. 노트북에서도 `boxplot(marketing_total ~ pop_density)`로 인구밀도(범주)별 마케팅비(숫자) 분포를 비교함.

**문제 20.** `cor.test(marketing$google_adwords, marketing$revenues)` 의 출력에서 **p-value가 매우 작게(<0.05)** 나왔다면 해석으로 옳은 것은?

- ① 두 변수의 상관계수가 0이다
- ② 두 변수 사이의 (선형)상관이 통계적으로 유의하다 — 우연으로 보기 어렵다
- ③ 두 변수는 인과관계가 있다
- ④ 표본 크기가 작다는 뜻이다

> [!success]- 정답 및 해설
> **정답: ②**
> `cor.test`의 귀무가설은 "상관계수 = 0"임. p-value가 작으면 이를 기각 → **상관이 유의함**. 단, 유의한 상관이 곧 **인과**를 뜻하지는 않음(③ 오답). 다음 문항의 가성상관과 연결해 생각할 것.

**문제 21.** 치즈 소비량(`cheese`)과 토목공학 박사 수(`degrees`)의 상관계수가 0.9를 크게 웃돌게 나옴. 그런데 둘은 인과관계가 없음. 이런 현상을 무엇이라 하며, 주된 원인은?

- ① 측정 오차 — 데이터가 잘못 입력됨
- ② 다중공선성 — 설명변수끼리 겹침
- ③ 표본이 너무 커서 생긴 착시
- ④ 가성상관(spurious correlation) — 두 변수가 각각 시간에 따라 증가하는 공통 추세를 가져 높은 상관이 나타남

> [!success]- 정답 및 해설
> **정답: ④**
> 서로 무관한 두 변수라도 **둘 다 시간에 따라 우상향**하면 상관계수가 높게 나옴 — 이를 **가성(허위)상관**이라 함. "상관 ≠ 인과"의 대표 사례임. 공통 추세(또는 제3의 잠복변수)가 원인임.

**문제 22.** 가성상관을 판별하기 위해 `lm(degrees ~ cheese)`의 **잔차에 `adf.test()`** 를 적용했음. 잔차가 **비정상(non-stationary)** 이고 한쪽으로 흐른다면 어떤 결론이 적절한가?

- ① 회귀가 매우 신뢰할 만하다
- ② 잔차가 정규분포를 따른다
- ③ 두 변수의 관계가 가성상관일 가능성이 크다
- ④ 상관계수를 다시 계산해야 한다

> [!success]- 정답 및 해설
> **정답: ③**
> 회귀 잔차가 **정상(stationary)** 이면 두 변수가 함께 움직이는 안정적 관계(공적분)로 볼 여지가 있지만, 잔차가 **비정상**이고 추세를 따라 흐르면 그 회귀는 **가성회귀**일 가능성이 큼. `adf.test`는 잔차의 정상성(단위근 유무)을 점검하는 도구임.

**문제 23.** `pairs(marketing)` 의 출력으로 옳은 설명은?

- ① 모든 변수쌍에 대한 산점도를 격자(행렬) 형태로 한 번에 보여 준다
- ② 변수 하나의 히스토그램만 그린다
- ③ 상관계수의 값만 표로 출력한다
- ④ 회귀계수를 계산해 준다

> [!success]- 정답 및 해설
> **정답: ①**
> `pairs()`는 **산점도 행렬(scatterplot matrix)** 을 그림. 변수쌍마다 산점도를 격자로 배치해, 어떤 쌍이 직선적 관계를 보이는지 한눈에 훑을 수 있음. 다변량 탐색의 출발점임.

**문제 24.** `corrgram(..., upper.panel = panel.pie)` 에서 한 칸의 **파이가 거의 꽉 찬(짙은) 원**으로 그려졌다면?

- ① 두 변수의 상관이 0에 가깝다
- ② 두 변수의 상관이 강하다(절댓값이 1에 가까움)
- ③ 결측치가 많다
- ④ 두 변수가 범주형이다

> [!success]- 정답 및 해설
> **정답: ②**
> `corrgram`의 파이 패널은 **상관의 크기를 채워진 비율**로, 색은 부호(양/음)를 나타냄. 파이가 많이 채워질수록 상관의 절댓값이 큼. 색·타원·파이 같은 시각 요소로 강한 상관쌍을 빠르게 찾는 것이 목적임.

**문제 25.** `nbike <- bike[ , -c(1:5, 13)]` 코드의 동작으로 옳은 것은?

- ① 1~5번과 13번 **행**을 남긴다
- ② 1~5번 열만 남긴다
- ③ 1~5번과 13번 **열**을 제거하고 나머지 열만 남긴다
- ④ 13개의 열을 무작위로 고른다

> [!success]- 정답 및 해설
> **정답: ③**
> 대괄호 `[행, 열]`에서 쉼표 뒤는 **열** 지정이고, **음수 인덱스는 "제외"** 를 뜻함. 즉 1~5열과 13열을 빼고 나머지(주로 숫자형 열)만 남겨 상관분석에 사용함. 음수 인덱싱은 "빼고 싶은 것을 지정"하는 R 특유의 방식임.

**문제 26.** `cor(marketing[,1:6])` 와 `corr.test(marketing[,1:6])` 의 출력 차이로 옳은 것은?

- ① 둘은 완전히 같은 출력을 준다
- ② `cor`는 상관계수 행렬만, `corr.test`는 상관계수에 더해 유의확률(p-value) 등 검정 결과까지 제공한다
- ③ `corr.test`는 상관계수를 주지 않는다
- ④ `cor`만 p-value를 준다

> [!success]- 정답 및 해설
> **정답: ②**
> `cor()`는 **상관계수 행렬**만 반환함. `psych::corr.test()`는 상관계수에 더해 **각 쌍의 p-value(유의성)** 와 신뢰구간 등을 함께 제공해, "이 상관이 통계적으로 의미 있는가"까지 판단하게 해 줌.

---

## D. 선형회귀 (27~38)

**문제 27.** `model1 <- lm(revenues ~ marketing_total, data = adverts)` 의 `summary(model1)`에서 `marketing_total`의 계수(기울기) 추정값이 양수로 유의하게 나왔음. 해석으로 가장 적절한 것은?

- ① 마케팅 총액이 1단위 늘 때 매출이 평균적으로 그 계수만큼 증가하는 경향
- ② 마케팅 총액과 매출은 무관하다
- ③ 매출이 늘면 마케팅 총액이 줄어든다
- ④ 계수는 매출의 절댓값이다

> [!success]- 정답 및 해설
> **정답: ①**
> 단순회귀의 기울기 계수는 "**설명변수 X가 1단위 증가할 때 반응변수 Y의 평균 변화량**"임. 양수이고 유의하면 마케팅 총액이 커질수록 매출이 평균적으로 증가하는 경향을 뜻함. 절편은 X=0일 때의 기대 Y임.

**문제 28.** `summary(model1)` 의 **R-squared(결정계수)** 가 0.8이라는 것의 의미는?

- ① 예측이 80% 확률로 맞는다
- ② 매출(Y)의 전체 변동 중 약 80%를 이 모형(마케팅 총액)이 설명한다
- ③ 오차가 80%다
- ④ 계수가 0.8이다

> [!success]- 정답 및 해설
> **정답: ②**
> R²는 **반응변수의 총변동 중 모형이 설명하는 비율**(0~1)임. 0.8이면 Y 변동의 약 80%를 설명함. "예측 적중 확률"이나 "오차 비율"이 아님에 주의.

**문제 29.** 노트북의 A(Actual)·P(Predicted) 그림에서 점선으로 표시된 **A와 P의 수직 거리**가 나타내는 것은?

- ① 회귀계수
- ② R-squared
- ③ 표준편차
- ④ 잔차(residual) = 실제값 − 예측값

> [!success]- 정답 및 해설
> **정답: ④**
> 한 관측치의 **실제값(A)** 과 회귀직선 위 **예측값(P)** 의 수직 거리가 **잔차**임. 최소제곱법은 이 잔차들의 **제곱합을 최소화**하도록 직선을 그음. 잔차 개념은 이후 가정 진단의 토대가 됨.

**문제 30.** 선형회귀의 3대 가정과 이를 점검하는 그림을 옳게 짝지은 것은?

- ① 정규성 — 잔차 히스토그램/Q-Q plot
- ② 등분산성 — 적합값 대 잔차 산점도
- ③ 선형성 — 산점도에 회귀직선
- ④ ①·②·③ 모두 옳다

> [!success]- 정답 및 해설
> **정답: ④**
> 선형성은 산점도+회귀직선으로, 정규성은 잔차의 히스토그램/Q-Q plot으로, 등분산성은 적합값 대 잔차 산점도(깔때기 모양인지)로 점검함. 세 가지 모두 옳게 짝지어짐.

**문제 31.** `jb.norm.test()`(정규성), `bptest()`(등분산), `bgtest()`(자기상관) 세 검정을 해석할 때 **공통으로 주의할 점**은?

- ① p-value가 작을수록 가정이 잘 만족된다
- ② 세 검정은 R-squared를 검정한다
- ③ 세 검정 모두 "가정이 성립한다"가 귀무가설이라, **p-value가 크면(>0.05) 가정 위배가 아니라고 본다**
- ④ p-value가 1이면 모형이 틀린 것이다

> [!success]- 정답 및 해설
> **정답: ③**
> 이들 진단검정의 귀무가설은 각각 "정규분포다 / 등분산이다 / 자기상관 없다"임. 따라서 **p-value가 크면 귀무가설을 기각하지 못해 "가정 위배 증거 없음"** 으로 해석함. 일반적인 "p가 작아야 유의" 직관과 **방향이 반대**라 자주 틀리는 부분임.

**문제 32.** 잔차의 **Q-Q plot**에서 점들이 대각 기준선에서 양 끝이 크게 휘어져 벗어났다면?

- ① 등분산성이 위배됐다
- ② 잔차가 정규분포에서 벗어났다(특히 꼬리 부분)
- ③ 선형성이 완벽하다
- ④ 자기상관이 있다

> [!success]- 정답 및 해설
> **정답: ②**
> Q-Q plot은 잔차 분위수를 정규분포 분위수와 비교함. 점이 기준선에 잘 붙으면 정규성에 부합, **양 끝이 휘면 꼬리가 두껍거나 치우친 분포** 등 정규성 위배를 시사함. 등분산·자기상관은 다른 그림/검정으로 봄.

**문제 33.** 적합값(fitted) 대 잔차(residual) 산점도가 **오른쪽으로 갈수록 점들이 위아래로 더 넓게 벌어지는 깔때기 모양**이라면?

- ① 등분산 가정이 위배된다(이분산, heteroscedasticity)
- ② 등분산 가정이 잘 성립한다
- ③ 잔차가 정규분포다
- ④ 모형의 R²가 1이다

> [!success]- 정답 및 해설
> **정답: ①**
> 등분산이면 잔차가 적합값과 무관하게 **일정한 폭**으로 흩어져야 함. 깔때기처럼 폭이 커지면 **이분산** — 등분산 위배임. `bptest()`로 수치 확인 가능. 항공승객 데이터가 대표적 이분산 사례임.

**문제 34.** `predict.lm(model1, newdata, interval = "predict")` 와 `interval = "confidence"` 의 차이로 옳은 것은?

- ① 예측구간(predict)이 신뢰구간(confidence)보다 항상 더 넓다
- ② 신뢰구간이 예측구간보다 항상 더 넓다
- ③ 두 구간의 너비는 같다
- ④ 예측구간은 계수의 범위, 신뢰구간은 개별 값의 범위다

> [!success]- 정답 및 해설
> **정답: ①**
> **신뢰구간**은 주어진 X에서 **평균 반응**의 불확실성만 반영해 좁음. **예측구간**은 거기에 더해 **개별 관측치의 추가 오차**까지 포함하므로 **더 넓음**. 같은 X·신뢰수준이면 예측구간 ⊃ 신뢰구간임.

**문제 35.** 같은 점에 대해 `level = 0.90`, `level = 0.99`로 예측구간을 구했을 때 너비 비교로 옳은 것은?

- ① 0.90 구간이 더 넓다
- ② 둘이 같다
- ③ 0.99 구간이 더 넓다
- ④ 신뢰수준은 너비와 무관하다

> [!success]- 정답 및 해설
> **정답: ③**
> 신뢰수준이 높을수록(더 확신하려 할수록) 더 많은 가능성을 포함해야 하므로 **구간이 넓어짐**. 따라서 99% 구간이 90% 구간보다 넓음. "더 확신 = 더 넓은 구간"의 트레이드오프임.

**문제 36.** 곡선 형태의 데이터에서 `y0^2`로 **변환**한 뒤 회귀를 다시 적합했더니 선형성·등분산이 개선됨. 이 변환의 목적은?

- ① 데이터 개수를 늘리려고
- ② 비선형 관계나 분산 불균등을 선형회귀 가정에 맞도록 형태를 바로잡으려고
- ③ R²를 인위적으로 0으로 만들려고
- ④ 결측치를 제거하려고

> [!success]- 정답 및 해설
> **정답: ②**
> 관계가 곡선이거나 분산이 일정치 않을 때 **로그·제곱·제곱근 등 변환**으로 선형성과 등분산을 회복시킬 수 있음. 변환 후 진단 그림(선형성·정규성·등분산)이 개선되면 변환이 적절했다는 신호임.

**문제 37.** 노트북은 "가운데에 있는 이상치(fit2)"와 "끝(x=30)에 있는 이상치(fit3)"를 비교함. **영향점(influential point)** 개념에 대한 설명으로 옳은 것은?

- ① 회귀직선(기울기)을 크게 바꾸는 이상치를 특히 영향점이라 하며, 보통 X축 양 끝(레버리지가 큰 위치)의 점이 더 큰 영향을 준다
- ② 모든 이상치는 회귀직선을 똑같이 크게 바꾼다
- ③ 이상치는 항상 제거해야 한다
- ④ 가운데 이상치가 항상 더 위험하다

> [!success]- 정답 및 해설
> **정답: ①**
> 이상치라고 모두 같은 영향을 주는 것은 아님. **X 범위의 끝(레버리지가 큰 위치)** 에 있는 점은 기울기를 크게 흔들어 **영향점**이 되기 쉬움. 이상치는 무조건 삭제가 아니라 **영향력을 진단**(예: `plot(model)`의 잔차-레버리지)한 뒤 판단함.

**문제 38.** 다중회귀 `lm(revenues ~ google_adwords + facebook + twitter)` 에서 `facebook`의 계수 해석으로 가장 정확한 것은?

- ① facebook 단독으로 매출과의 단순상관
- ② 세 변수 계수의 평균
- ③ facebook이 전체 매출에서 차지하는 비율
- ④ **다른 두 변수(google_adwords, twitter)를 일정하게 고정했을 때** facebook이 1단위 늘 때 매출의 평균 변화량

> [!success]- 정답 및 해설
> **정답: ④**
> 다중회귀의 각 계수는 **나머지 설명변수를 통제(고정)한 상태에서의** 부분효과임. 즉 google_adwords와 twitter가 같을 때 facebook만 1단위 늘었을 때의 매출 평균 변화임. 단순상관(①)과 다르며, 다중회귀에서도 정규성·등분산·자기상관 진단을 동일하게 거쳐야 함.

---

## E. 시계열 분석 (39~50)

**문제 39.** `monthly <- ts(riders, frequency = 12, start = c(2011, 1))` 에서 `frequency = 12`의 의미는?

- ① 12개의 관측치만 쓴다
- ② 1주기가 12개 시점(즉 월별 데이터, 1년 = 12개월)임을 뜻한다
- ③ 12년치 데이터다
- ④ 12번 반복 측정했다

> [!success]- 정답 및 해설
> **정답: ②**
> `ts`의 `frequency`는 **한 계절 주기 안의 관측 수**임. 월별 데이터는 1년에 12개이므로 `frequency = 12`. `start = c(2011, 1)`은 2011년 1월 시작을 뜻함. 이 주기 정보가 있어야 `decompose`·계절 차분 등이 올바로 작동함.

**문제 40.** `plot(decompose(airpass))` 출력의 네 패널 중 **seasonal** 패널이 보여 주는 것은?

- ① 장기적으로 우상향하는 추세
- ② 설명되지 않는 무작위 잡음
- ③ 매년 규칙적으로 반복되는 계절 변동 성분
- ④ 원본 데이터 그대로

> [!success]- 정답 및 해설
> **정답: ③**
> `decompose`는 시계열을 observed(원본)·trend(추세)·seasonal(계절성)·random(불규칙)으로 나눔. **seasonal 패널은 주기(12개월)마다 반복되는 규칙적 변동**을 보여 줌. 추세는 trend, 잡음은 random 패널에 나타남.

**문제 41.** 항공 승객(airpass)을 단순 선형회귀로만 분석하면 부족한 이유로 가장 적절한 것은?

- ① 데이터가 너무 적어서
- ② R에서 ts 객체에는 lm을 못 써서
- ③ 회귀는 예측을 못 해서
- ④ 시간이 갈수록 변동 폭이 커지고(이분산) 강한 계절성·시간 의존성이 있어, 직선 추세만으로는 구조를 못 담기 때문

> [!success]- 정답 및 해설
> **정답: ④**
> airpass는 우상향 추세에 더해 **연 단위 계절성**과 **시간이 갈수록 커지는 변동(이분산)** 을 가짐. 직선 회귀는 추세만 잡고 계절성·시간 의존성을 설명하지 못해 잔차 진단이 위배됨 → 분해·차분·ARIMA 같은 시계열 전용 기법이 필요함.

**문제 42.** `diff(monthly)` 와 `diff(diff(monthly), lag = 12)` 가 각각 제거하는 성분으로 옳은 것은?

- ① `diff()`는 추세, `lag=12` 차분은 12개월 주기의 계절성
- ② `diff()`는 계절성, `lag=12` 차분은 추세
- ③ 둘 다 추세만 제거
- ④ 둘 다 계절성만 제거

> [!success]- 정답 및 해설
> **정답: ①**
> 1차 차분 `diff()`는 이웃 시점의 차이를 취해 **추세**를 제거함. `lag = 12` 차분은 12개월 전과의 차이를 취해 **연간 계절성**을 제거함. 둘을 결합하면 시계열이 **정상성**에 가까워져 ARIMA 적합이 쉬워짐.

**문제 43.** 시계열을 ARIMA로 모형화하기 전에 **차분으로 정상성(stationarity)** 을 확보하려는 이유는?

- ① 그래프를 예쁘게 만들기 위해
- ② 데이터 양을 줄이려고
- ③ ARIMA류 모형은 평균·분산·자기상관 구조가 시간에 따라 일정한(정상) 시계열을 전제로 하기 때문
- ④ 계절성을 강화하려고

> [!success]- 정답 및 해설
> **정답: ③**
> ARIMA의 AR·MA 부분은 **정상 시계열**(시간에 무관하게 통계적 성질이 일정)을 가정함. 추세·계절성이 있으면 비정상이므로 차분(d, 계절차분 D)으로 정상화한 뒤 모형화함. ARIMA의 'I'(Integrated)가 바로 이 차분을 뜻함.

**문제 44.** `acf(monthly)` 와 `pacf(monthly)` 를 그리는 목적으로 옳은 것은?

- ① 결측치를 찾기 위해
- ② ARIMA(p, d, q)의 차수 단서를 얻기 위해 — ACF는 MA 차수(q), PACF는 AR 차수(p) 식별에 도움
- ③ 정규성을 검정하기 위해
- ④ 계절성을 제거하기 위해

> [!success]- 정답 및 해설
> **정답: ②**
> **ACF(자기상관함수)** 는 MA 차수 q, **PACF(편자기상관함수)** 는 AR 차수 p를 가늠하는 단서를 줌. 어느 시차에서 상관이 뚝 끊기는지(절단)·완만히 줄어드는지(감쇠)를 보고 후보 차수를 정함.

**문제 45.** `fit2 <- Arima(monthly, c(1,1,0), ...)` 에서 가운데 `1`이 의미하는 것은?

- ① AR 차수(p)
- ② MA 차수(q)
- ③ 계절 주기
- ④ 차분 횟수(d)

> [!success]- 정답 및 해설
> **정답: ④**
> `Arima(데이터, c(p, d, q))`에서 순서대로 p=AR, **d=차분 횟수**, q=MA임. `c(1,1,0)`은 AR(1)·1차 차분·MA 없음을 뜻함. 즉 가운데 값이 비정상 시계열을 정상화하기 위한 차분 차수임.

**문제 46.** 여러 ARIMA 후보를 `tsdiag()`로 진단하고 **AIC**를 비교함. 더 나은 모형을 고르는 기준으로 옳은 것은?

- ① AIC가 클수록, 잔차에 자기상관이 많을수록 좋다
- ② R²만 보면 된다
- ③ AIC가 작을수록 좋고, 잔차에 유의한 자기상관이 남아 있지 않아야 좋다
- ④ 계수의 개수가 많을수록 좋다

> [!success]- 정답 및 해설
> **정답: ③**
> **AIC는 작을수록**(적합도와 단순성의 균형이 좋을수록) 선호됨. 또한 `tsdiag`의 잔차 ACF·Ljung-Box p-value에서 **잔차에 자기상관이 남지 않아야**(백색잡음에 가까워야) 모형이 구조를 잘 설명한 것임. 두 기준을 함께 봄.

**문제 47.** `plot(forecast(fit2, h = 12))` 출력에서 예측선 주변의 **음영(밴드)** 과, 시점이 멀어질수록 밴드가 **넓어지는 현상**의 의미는?

- ① 음영은 예측의 신뢰구간이며, 미래로 갈수록 불확실성이 커져 구간이 넓어진다
- ② 음영은 실제 관측치이고, 넓어지는 건 오류다
- ③ 음영은 계절성이고 넓어지는 건 추세다
- ④ 음영은 잔차의 합이다

> [!success]- 정답 및 해설
> **정답: ①**
> `forecast` 그래프의 음영은 보통 80%·95% **예측 신뢰구간**임. 예측 시점이 멀어질수록 누적 불확실성이 커져 **밴드가 점점 넓어짐** — 먼 미래일수록 예측이 덜 확실하다는 직관과 일치함.

**문제 48.** `fit6 <- auto.arima(monthly)` 의 역할과 올바른 사용 태도는?

- ① 사람이 차수를 고를 필요를 완전히 없애 주므로 결과를 그대로 믿으면 된다
- ② 항상 수동 모형보다 나쁘다
- ③ 계절성은 절대 못 잡는다
- ④ AIC 등 기준으로 차수를 자동 탐색해 주지만, 잔차 진단(`tsdiag`)과 계수 유의성은 여전히 직접 확인하는 것이 바람직하다

> [!success]- 정답 및 해설
> **정답: ④**
> `auto.arima`는 차수 조합을 자동 탐색해 편리하지만 만능은 아님. 자동 선택 결과도 **잔차가 백색잡음인지, 계수가 유의한지** 직접 검증해야 함. 자동화는 출발점이지 종착점이 아님.

**문제 49.** 추정계수의 유의성을 보려고 `z <- fit6$coef / sqrt(diag(fit6$var.coef))`, `pvalue <- 2*(1 - pnorm(abs(z)))` 를 계산했음. 어떤 계수에 대한 **p-value가 0.5처럼 크게** 나왔다면?

- ① 그 계수는 매우 유의하다
- ② 그 계수가 0과 통계적으로 다르다고 보기 어렵다(유의하지 않다)
- ③ 그 계수의 부호가 반대다
- ④ 모형 전체가 틀렸다

> [!success]- 정답 및 해설
> **정답: ②**
> `z = 추정계수 / 표준오차`, 양측검정 `p = 2(1 − Φ(|z|))`임. p-value가 크면 "계수 = 0"이라는 귀무가설을 기각하지 못함 → **그 항은 유의하지 않음**. 유의하지 않은 항이 많으면 더 단순한 차수의 모형을 고려할 단서가 됨.

**문제 50.** TBATS 예측 결과로 2011·2012년 실제 월평균과 2013년 예측 월평균(`mean_2011`, `mean_2012`, `mean_2013`)을 구해 그래프에 평균선으로 겹쳐 그렸음. 만약 `mean_2013`이 두 해 실제 평균보다 높게 나왔다면, 비즈니스 관점의 해석으로 가장 적절한 것은?

- ① 대여 수요가 감소세로 돌아설 것이다
- ② 2013년 데이터가 이미 관측되었다는 뜻이다
- ③ 모형이 다음 해에도 평균 대여량이 증가하는 성장 추세를 예측한다 — 다만 예측 신뢰구간 폭을 함께 고려해 해석해야 한다
- ④ 평균선은 계절성을 의미한다

> [!success]- 정답 및 해설
> **정답: ③**
> 연도별 평균선이 우상향(2011 < 2012 < 2013 예측)하면 모형은 **성장 추세**를 전망하는 것임. 이는 증설·재고·마케팅 같은 의사결정의 근거가 될 수 있음. 단, 예측에는 불확실성(신뢰구간)이 있으므로 **점 추정값만이 아니라 구간 폭까지** 함께 보고 판단해야 함. 또한 이 평균선은 추세 요약이지 계절성 자체가 아님.

---

## 정답 빠른 확인표

| 문항 | 정답 | 문항 | 정답 | 문항 | 정답 | 문항 | 정답 | 문항 | 정답 |
|----|----|----|----|----|----|----|----|----|----|
| 1 | ③ | 11 | ③ | 21 | ④ | 31 | ③ | 41 | ④ |
| 2 | ① | 12 | ② | 22 | ③ | 32 | ② | 42 | ① |
| 3 | ② | 13 | ④ | 23 | ① | 33 | ① | 43 | ③ |
| 4 | ④ | 14 | ① | 24 | ② | 34 | ① | 44 | ② |
| 5 | ① | 15 | ③ | 25 | ③ | 35 | ③ | 45 | ④ |
| 6 | ③ | 16 | ① | 26 | ② | 36 | ② | 46 | ③ |
| 7 | ④ | 17 | ④ | 27 | ① | 37 | ① | 47 | ① |
| 8 | ② | 18 | ② | 28 | ② | 38 | ④ | 48 | ④ |
| 9 | ③ | 19 | ③ | 29 | ④ | 39 | ② | 49 | ② |
| 10 | ④ | 20 | ② | 30 | ④ | 40 | ③ | 50 | ③ |

> 💡 실제 시험 대비 시에는 정답을 가리고 풀어 본 뒤, 틀린 문항이 속한 챕터의 본문을 다시 읽는 방식이 효과적임.

---

강의 개요로 → [[index|경영빅데이터분석]]
